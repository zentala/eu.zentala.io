{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Prepare Wordspace",
        "type": "shell",
        "command": "source ~/.nvm/nvm.sh && nvm use && npm install && npm run start && open http://localhost:8000/",
        "runOptions": { "runOn": "folderOpen" },
        "presentation": {
          "group": "Development Servers"
        }
      }
    ]
  }
  